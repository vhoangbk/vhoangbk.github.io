{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "node vercel-build.js",
  "routes": [
    { "src": "/(.*)", "dest": "/dist/(.*)" }
  ],
  "env": {
    "NODE_ENV": "production",
    "VERCEL": "1"
  }
}